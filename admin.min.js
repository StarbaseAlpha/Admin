"use strict";function Admin(api,auth){if(!api)throw"Starbase Channels API Client object is missing.";if(!auth)throw"Starbase Authentication object is missing.";return async req=>{let{path:path,method:method,data:data}=req,token=null;return token=req.token?req.token:await auth.getToken().catch(err=>null),api.setToken(token),api.path(path)[method](data)}}